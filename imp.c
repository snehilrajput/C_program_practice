//pointer arithmetic
////ptr++ : pointer is incremented
//++ptr : pointer is incremented
//*ptr++ :pointer is incremented
//++*ptr : value is incremented
//(*ptr)++: value is incremented
//++(*ptr): value is incremented

// (*p)++ → print first, increase later
// ++(*p) → increase first, then print


// 👉 If ++ is before * → value changes
// 👉 If ++ is inside () with pointer → pointer moves

// 🔥 Ultra Shortcut (Revise Fast)

// 👉 *p++ = value, then move pointer
// 👉 ++*p = increase value
// 👉 *(++p) = move pointer, then value
// 👉 (*p)++ = use value, then increase

//strcmp(...) == 0Means strings are equal → evaluates as false in if
//if(0)Always false in C