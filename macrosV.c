// Macros variable declarations
#include<stdio.h>
#define row 3 // marcos used for change row and col values 
#define col 4 // so preprocessor will remove # and handle it change varibles into values 
#define SIZE 7 
#define PI 3.14
int main(){
    int arr1[SIZE];
    int arr[row][col];
    printf("PI = %d\n", PI);
    for(int i = 0; i<row; i++){
        for(int j = 0; j< col; j++){
            //scanf
        }
    }
    for(int i = 0; i<row; i++){
        for(int j = 0; j<col; j++){
          // printf
        }
    }
return 0;
}